# Compiled system-audio helper — built locally by syscap/build.sh (and install.sh)
jakelisten-syscap

# Node
node_modules/
npm-debug.log*
yarn-error.log

# Recordings, transcripts, and local config never live in the repo, but ignore
# them anyway in case anyone runs the tool from inside the checkout.
recordings/
*.wav
*.caf
*.transcript.txt
*.summary.txt
config.json

# macOS
.DS_Store
